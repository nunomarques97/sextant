"""Pure domain types.

This package holds value objects and enums only. It performs no I/O, opens no
sockets, reads no files and imports nothing else from ``sextant``. The layering
contract in ``.importlinter`` enforces that.
"""
