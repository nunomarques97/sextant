"""Regime classification. Not implemented in SEXTANT-001.

The deterministic classifier is the primary source. An LLM regime label is an
additional opinion recorded alongside it, never a replacement for it, so that
disagreement between the two can be measured rather than assumed away.
"""
